# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 排除，不会入库）

# MySQL 连接串：换成你本机的用户名和密码
DATABASE_URL=mysql+pymysql://root:你的MySQL密码@localhost:3306/beans_db?charset=utf8mb4

# JWT 签名密钥：随便一段足够长的随机字符串
JWT_SECRET=change-me-to-a-long-random-string
